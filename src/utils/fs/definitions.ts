import path from "node:path";

//----------------------------------------------------------------------------//

export const ROOT_DIR = path.join(__dirname, "..", "..", "..");

export const FILES_DIR = path.join(ROOT_DIR, "files");

export const FONTS_DIR = path.join(FILES_DIR, "font");

//----------------------------------------------------------------------------//
